{
	"name": "promern_stack",
	"version": "1.0.0",
	"description": "tutorial from book promern stack",
	"main": "server.js",
	"scripts": {
		"start": "nodemon -w server.js",
		"compile": "babel src --presets react,es2015 --out-dir static",
		"watch": "babel src --presets react,es2015 --out-dir static --watch",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"babel-cli": "^6.26.0",
		"babel-preset-react": "^6.24.1",
		"express": "^4.16.4"
	},
	"devDependencies": {
		"babel-preset-es2015": "^6.24.1",
		"nodemon": "^1.18.10"
	}
}